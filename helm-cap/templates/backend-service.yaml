apiVersion: v1
kind: Service
metadata:
  name: mongodb-svc
  namespace: cap-ns
spec:
  selector:
    app: mongodb
  ports:
    - port: 27017
      targetPort: 27017
  type: ClusterIP
  clusterIP: None